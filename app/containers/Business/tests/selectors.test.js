// import { fromJS } from 'immutable';
// import { makeSelectBusinessDomain } from '../selectors';

// const selector = makeSelectBusinessDomain();

describe('makeSelectBusinessDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
